# ⚖️ Thai Legal RAG System (ระบบถาม-ตอบข้อกฎหมายไทยด้วย AI)

โปรเจกต์นี้เป็นการพัฒนาระบบ Retrieval-Augmented Generation (RAG) ที่ออกแบบมาเพื่อการตอบคำถามข้อกฎหมายไทยโดยเฉพาะ โดยมุ่งเน้นความแม่นยำทางข้อเท็จจริง (Factual Accuracy) การตีความเชิงตรรกะแบบกฎหมาย (Legal Logic) และการลดอาการหลอนของโมเดล (Hallucination)

---

## Architecture โดยรวม (System Design)

สถาปัตยกรรมของระบบถูกแบบออกเป็น 4 ส่วนหลัก ดังนี้:

1.  **Data Ingestion (การนำเข้าข้อมูล):** ดึงข้อมูลเอกสารกฎหมายจากไฟล์ JSONL ทำการทำความสะอาดข้อความ (Text Cleaning) จัดการรูปแบบคำว่า "มาตรา..." ให้อยู่ในฟอร์แมตมาตรฐาน และเก็บ Metadata เช่น ชื่อพระราชบัญญัติและปี พ.ศ. ลงในฐานข้อมูล
2.  **Hybrid Retrieval (การค้นหาข้อมูล):** ผสมผสานเทคนิคการค้นหาสองรูปแบบเพื่ออุดรอยรั่วของกันและกัน:
    *   **Vector Search (Semantic):** ค้นหาด้วยความหมายโดยใช้ `HuggingFaceEmbeddings` ผ่าน `ChromaDB`
    *   **Keyword Search (Lexical):** ใช้ตัวดึงข้อมูล `BM25` ร่วมกับตัวตัดคำภาษาไทย (`PyThaiNLP`) เพื่อรับประกันว่าจะไม่พลาดคำสำคัญที่มีความเฉพาะเจาะจง (เช่น เลขมาตรา)
    *   **Reciprocal Rank Fusion (RRF):** นำผลลัพธ์จากทั้งสองมารวมคะแนนและจัดอันดับใหม่
    *   **Hard Metadata Filtering:** กรองเอกสารด่านสุดท้ายด้วย Metadata เพื่อป้องกันการดึงกฎหมายข้ามฉบับ (เช่น กฎหมายประมง มาปนกับ กฎหมายสถาบันการเงิน)
3.  **Generation (การสร้างคำตอบ):** ใช้ Local LLM (รันผ่าน `Ollama`) เป็นเครื่องยนต์หลักในการตอบคำถาม โดยใช้เทคนิค **Few-Shot Prompting** สอนให้โมเดลเข้าใจตรรกะปฏิเสธซ้อนปฏิเสธของกฎหมายไทย และบังคับให้ให้ฟันธงตอบอย่างตรงไปตรงมา
4.  **Evaluation (การประเมินผล):** ใช้เทคนิค **LLM-as-a-Judge** ขับเคลื่อนด้วยกระบวนการ Chain-of-Thought (CoT) บังคับให้ Judge ยกคำพูด (Quote) ของ AI มาวิเคราะห์เหตุผลก่อนตัดสิน PASS/FAIL เพื่อลด False Positives/Negatives

---

## การเลือกใช้ Library (Tech Stack)

*   **[LangChain](https://python.langchain.com/):** เป็น Framework หลักในการทำ Orchestration เชื่อมต่อ Document Loaders, Vector Stores, Prompts, รูปแบบ Output Parsers และ Retriever ประกอบร่างเป็น AI Chain
*   **[ChromaDB](https://www.trychroma.com/):** Vector Database แบบ Local ที่ไม่ต้องพึ่งพา Cloud Setup เหมาะสำหรับการรันแบบ Offline (Privacy 100%) และการสร้าง Proof of Concept อย่างรวดเร็ว
*   **[Ollama](https://ollama.com/):** สำหรับบริหารจัดการ Local LLM (เช่น Llama 3.2 3B) รองรับการโต้ตอบที่รวดเร็ว (Low Latency) และลดค่าใช้จ่าย API
*   **[HuggingFace Embeddings](https://huggingface.co/):** ใช้โมเดล Embedding แบบ Local (เช่น `bge-m3`) แทนการเรียกใช้ OpenAI API เพื่อจำกัดขอบเขตการทำงานให้อยู่แค่ในเครื่อง
*   **[PyThaiNLP](https://pythainlp.github.io/):** ไลบรารีสำคัญต่องานประมวลผลภาษาไทย ถูกเรียกใช้เป็น `preprocess_func` ให้กับ `BM25Retriever` เพื่อแยกคำไทยที่เขียนติดกันโดยไม่มีช่องว่างให้ถูกต้อง

---

## 🇹🇭 วิธีจัดการภาษาไทยและบริบทกฎหมาย (Thai Language Handling)

ภาษาไทย โดยเฉพาะภาษากฎหมาย มีความซับซ้อนและดิ้นได้หลายรูปแบบ ระบบจึงต้องมีกลไกจัดการพิเศษ:

1.  **การจัดการ "มาตรา" (Section Numbering):**
    *   ใช้ Regular Expressions (RegEx) ตรวจจับและจัดรูปแบบ Metadata `section_id` แปลงรหัสเช่น `ID:M_6` หรือข้อความที่ตัดแหว่ง ให้กลายเป็น `"มาตรา: 6"` อย่างเป็นระเบียบ และฝังชื่อกฎหมาย `กฎหมาย: [ชื่อพระราชบัญญัติ]` ติดไว้ที่หัวชั๊งค์ข้อมูล (Chunk) เสมอ เพื่อลดความสับสนของ Vector และ LLM
2.  **ปัญหา Hybrid Search ตัดคำไทย (Thai Tokenization for BM25):**
    *   อัลกอริทึม BM25 ปกติตัดคำด้วยเว้นวรรค (Space) ซึ่งใช้กับภาษาไทยไม่ได้ เราจึงเขียน Custom Retriever ให้ `BM25` เรียกใช้ปืนกลตัดคำ `pythainlp.tokenize.word_tokenize` ก่อนการทำ Indexing
3.  **Prompt Engineering เพื่อแก้ตรรกะวิบัติ (Logic Alignment for Small LLM):**
    *   สอนศัพท์บัญญัติ: สอนกฎเหล็กว่าคำว่า **"มิให้"** แปลว่า **"ไม่/ห้าม"** อย่างเด็ดขาด
    *   สอนเรื่องเวลา (Temporal Logic): หากกฎหมายบอกว่า "ต้องได้รับอนุญาตก่อน" AI ต้องไม่ตอบว่า "ได้ แต่ต้องขออนุญาต" (ตรรกะย้อนแย้ง) แต่ต้องฟันธงว่าเบื้องต้นคือ **"ไม่ได้/ไม่ใช่"**

---

## ตัวอย่างข้อมูล (Example Data Format)

ตัวอย่างชุดข้อมูลที่ใช้ประเมินผล (Evaluation Dataset) ประกอบด้วยคำถาม บริบทที่เกี่ยวข้อง (Positive Contexts) และคำตอบที่ถูกต้อง (Ground Truth):

```json
{
  "question": "สถาบันทางเงินต้องการที่จะจัดตั้งหรือเลิก สามารถทำได้ทันทีหรือไม่",
  "positive_contexts": [
    "การจัดตั้ง การตั้งสาขา การย้ายที่ตั้ง การเลิกสาขา การร่วมกันใช้สถานที่ ... ให้เป็นไปตามหลักเกณฑ์ที่ธนาคารแห่งประเทศไทยอนุญาต"
  ],
  "positive_answer": "ไม่ได้ ต้องได้รับอนุญาตจากธนาคารแห่งประเทศไทย",
  "hard_negative_contexts": [
    "การเลิกบริษัทมหาชนจำกัดตามกฎหมายว่าด้วยบริษัทมหาชนจำกัดเพื่อประกอบธุรกิจตามวรรคหนึ่ง..."
  ]
}
```

---

## แหล่งอ้างอิงและชุดข้อมูล (References & Datasets)

ระบบนี้ดึงอิทธิพลแนวคิดและปรับแต่งโมเดลมาจากการศึกษาชุดข้อมูลกฎหมายไทยแบบเปิดเผย (Open Law Data Thailand) บนแพลตฟอร์ม Hugging Face:

1.  **[soc-ratchakitcha](https://huggingface.co/datasets/open-law-data-thailand/soc-ratchakitcha):** ชุดข้อมูลราชกิจจานุเบกษา
2.  **[ocs-krisdika](https://huggingface.co/datasets/open-law-data-thailand/ocs-krisdika):** ชุดข้อมูลกฎหมายจากสำนักงานคณะกรรมการกฤษฎีกา
3.  **[WangchanX-Legal-ThaiCCL-RAG](https://huggingface.co/datasets/airesearch/WangchanX-Legal-ThaiCCL-RAG):** ชุดข้อมูลสำหรับการเทรนและประเมินระบบ RAG กฎหมายไทยโดยศูนย์วิจัย AIResearch
